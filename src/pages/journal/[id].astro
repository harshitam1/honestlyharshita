---
import { blogPosts } from '../../data/blogData';

export async function getStaticPaths() {
  return blogPosts.map((post) => {
    return {
      params: { id: post.id },
      props: { post },
    };
  });
}

// Redirect to journal index if someone tries to access a post directly
return Astro.redirect('/journal');
---